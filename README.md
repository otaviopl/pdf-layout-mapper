# Field Mapping

Editor visual para mapear campos sobre templates PNG/JPG e exportar um JSON único em coordenadas A4/PT.

## Comandos

```bash
pnpm install
pnpm dev
```

Para gerar build:

```bash
pnpm build
```
